<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="100dp"
    android:paddingBottom="5dp">

    <ImageView
        android:id="@+id/record_image"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        app:srcCompat="@drawable/photo_item_background" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerVertical="true"
        android:layout_toEndOf="@+id/record_image">

        <TextView
            android:layout_marginLeft="10dp"
            android:id="@+id/record_listing_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ems="14"
            android:textStyle="bold"
            android:text="Listing Title" />

        <TextView
            android:id="@+id/record_price"
            android:layout_alignLeft="@id/record_listing_title"
            android:layout_below="@id/record_listing_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ems="10"
            android:textStyle="normal"
            android:text="price" />

    </RelativeLayout>
</RelativeLayout>

